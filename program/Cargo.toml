[package]
name = "winternitz-vault"
version = "0.1.0"
edition = "2021"
description = "SPL token mint authority and treasury guarded by one-time hash-based signatures"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "=2.2.1"
solana-security-txt = "1.1"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
panic = "abort"
